import { PageSchema } from './page';

export const examplePage: PageSchema = {
  id: 'page-1',
  root: [
    {
  id: "text-1",
  type: "text",
  props: { content: "Hello Builder 🚀" },
  children: []
},
  ],
};
